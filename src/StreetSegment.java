public class StreetSegment extends Edge implements WeightedEdge {
    
    public StreetSegment(Node source, Node destination, int weight) {
        super(source, destination, weight);
    }

    // L'implémentation de getWeight est déjà fournie par la classe mère Edge
}